## Agent skills

### Issue tracker

Before reading or changing issues and pull requests, follow the GitHub workflow in `docs/agents/issue-tracker.md`.

### Triage labels

When a skill uses triage category or state roles, resolve them through `docs/agents/triage-labels.md` before changing labels.

### Domain docs

Before codebase exploration, architecture work, naming, specs, or refactors, follow the single-context rules in `docs/agents/domain.md`.
