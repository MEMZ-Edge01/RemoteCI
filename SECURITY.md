# 安全说明

## 受支持版本

当前仅支持最新发布的 v0.3.x 系列；更早版本可能缺少修复，不承诺安全更新。

## 报告漏洞

请通过 GitHub 的**私密安全公告**（Security Advisory）提交漏洞，不要在 Issue、
PR 或聊天中公开细节：

1. 打开 [RemoteCI 安全公告](https://github.com/MEMZ-Edge01/RemoteCI/security/advisories/new)。
2. 填写漏洞描述、影响范围与复现信息，并注明“私密报告”。
3. 文档仓库的问题同样通过 RemoteCI 的安全公告提交，再在正文中关联
   [RemoteCI-Docs](https://github.com/MEMZ-Edge01/RemoteCI-Docs)。

收到报告后会尽快评估并回复；修复发布前请不要公开漏洞细节。

## 数据处理

本系统保存密码哈希、设备会话验证器等敏感数据。涉及认证、会话或权限的改动
必须额外说明其安全影响，并在合并前经过至少一次人工审核。

## 局域网信任模型

局域网直连与设备发现运行在**可信 Wi-Fi** 前提之上，请知悉以下边界：

- UDP 设备扫描（端口 48765）与 `/bootstrap` 端点均无认证，同一局域网内的设备
  可以伪造插件应答或云端地址。手表会原样展示获取到的云服务器地址，并对明文
  HTTP 地址给出风险提示；请只在受信任网络中使用“扫描局域网设备”，并在输入
  密码前确认云端地址正确。
- 局域网 WebSocket 使用一次性 HMAC 挑战认证，通道本身是明文 `ws://`；课程数据
  与命令可能被同网设备窃听。云服务器应配置 HTTPS（`wss://`）并启用 HSTS。
- 插件配置文件（`Settings.json`）与授权镜像（`Accounts.json`）以明文保存插件
  凭据与设备验证器，请限制运行 ClassIsland 主机的文件访问权限。
- 账号密码只会发送给配置的云服务器 HTTPS 端点，永不发送给插件或局域网端点。
